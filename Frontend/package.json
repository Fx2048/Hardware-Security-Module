{
  "name": "hsm-simulator-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build needed - static files'",
    "start": "npx serve -s . -p $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}